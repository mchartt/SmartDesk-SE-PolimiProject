# Password corta accettabile solo in test: H2 in-memory, mai esposto.
app.seed.admin.email=admin@test.local
app.seed.admin.password=Test_seed_1!
spring.profiles.active=test
security.access-token-secret=test-secret-at-least-32-chars-123456
security.access-token-ttl-seconds=900
security.jwt-issuer=smartdesk-backend-test
# E2E + slice tests build their own users; JSON seed is dev-only noise here.
app.seed.test-users.enabled=false